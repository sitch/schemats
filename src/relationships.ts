import { Config } from "./config";
import { Relationships } from "./adapters/types";
import { TableDefinitions } from "./adapters/types";

export const getRelationships = (
  config: Config,
  tables: TableDefinitions
): Relationships => {
  return [];
};
